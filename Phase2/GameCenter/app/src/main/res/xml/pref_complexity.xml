<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="4"
        android:entries="@array/pref_size_list_titles"
        android:entryValues="@array/pref_size_list_values"
        android:key="size_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_size" />

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/pref_undo_list_titles"
        android:entryValues="@array/pref_undo_list_values"
        android:key="undo_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_undo" />

</PreferenceScreen>
